type User {
  id: ID! @id
  name: String!
  password: String!
  email: String
  phone: String
  address: Address @relation(link: INLINE)
}

type Address {
  id: ID! @id
  nation: String!
  city: String!
  roadNameAndNumber: String
  user: User!
}
